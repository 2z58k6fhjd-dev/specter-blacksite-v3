# Conversion / compatibility status

## Current result

**No conversion is authorized or has occurred.** There is no local archive,
glTF, GLB, mesh, texture, animation, or runtime import for this candidate.

## Conditional future path

1. Freeze the original archive and create the source SHA-256 receipt.
2. Inspect the actual node/mesh hierarchy; do not trust a named-parts claim
   until magazine, bolt/handle, selector, trigger, muzzle, ejection, sights,
   and light-mount anchors are found in the file.
3. Convert only from the audited original to a browser-ready glTF/GLB with
   preserved PBR channels, local author/license record, and mesh/material
   provenance. Do not bake a texture upscale and call it native detail.
4. Generate documented quality tiers: close player model, lower enemy/world
   model, 2K default maps, and retained-texture 512px Low derivatives.
5. Fit the viewmodel with a visible stock seat, correct bore/sight line, true
   usable optic only if the model supplies a legitimate aperture, muzzle flash
   at the barrel end, ejection from the actual port, and a light below barrel.
6. Integrate only after browser performance, static closure, runtime actions,
   ADS, reload, death-drop, and third-person enemy carry QA pass.

## Explicit limits

This candidate must not be used to bypass the art gate, replace a model merely
because it is an AK, or be described as a high-detail SPECTER runtime asset
before visual review. It remains research metadata only.
