# SPECTER low-payload texture tier

This tree is a reproducible 512px-max derivative of bundled runtime textures. It is generated by `scripts/build-low-textures.py` and selected before high-resolution image decode on Competitive Low / Low-texture startup paths. All source licenses and attribution obligations remain with the original asset folders; `manifest.json` records source and derivative hashes.
